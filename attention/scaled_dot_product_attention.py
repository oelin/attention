"""Scaled Dot-product Attention.

This implementation is based on PyTorch's `scaled_dot_product_attention` function, which takes in
precomputed query, key and value tensors as well as an optional mask tensor. If `is_causal` is set

See:
See: 
